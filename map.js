const fruits = ['strawberry', 'grape', 'raspberry', 'watermelon', 'kiwi', 'lemon', 'blueberry', 'orange', 'grapefruit', 'banana'];

//// Example 1: Create a new array containing the length of each fruit
const fruitLengths = fruits.map(fruit => fruit.length)

console.log(lengthOfFruits);


// // Example 2: Create an array of objects containing both the fruit and its index
// const createFruitObject = fruits.map((fruit, index) => ({ fruit, index }));

// console.log(createFruitObject);


